# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "axhal"
version = "0.2.0"
authors = ["Yuekai Jia <equation618@gmail.com>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "ArceOS hardware abstraction layer, provides unified APIs for platform-specific operations"
homepage = "https://github.com/arceos-org/arceos"
documentation = "https://arceos-org.github.io/arceos/axhal/index.html"
readme = false
license = "GPL-3.0-or-later OR Apache-2.0 OR MulanPSL-2.0"
repository = "https://github.com/arceos-org/arceos/tree/main/modules/axhal"

[features]
default = []
defplat = [
    "dep:axplat-x86-pc",
    "dep:axplat-aarch64-qemu-virt",
    "dep:axplat-riscv64-qemu-virt",
    "dep:axplat-loongarch64-qemu-virt",
    "dep:axplat-arm-qemu-virt",
]
fp-simd = [
    "axcpu/fp-simd",
    "axplat-x86-pc?/fp-simd",
    "axplat-aarch64-qemu-virt?/fp-simd",
    "axplat-riscv64-qemu-virt?/fp-simd",
    "axplat-loongarch64-qemu-virt?/fp-simd",
    "axplat-arm-qemu-virt?/fp-simd",
]
ipi = ["irq"]
irq = [
    "linkme",
    "axplat-x86-pc?/irq",
    "axplat-aarch64-qemu-virt?/irq",
    "axplat-riscv64-qemu-virt?/irq",
    "axplat-loongarch64-qemu-virt?/irq",
    "axplat-arm-qemu-virt?/irq",
    "axplat/irq",
]
myplat = []
paging = [
    "axalloc",
    "page_table_multiarch",
]
rtc = [
    "axplat-x86-pc?/rtc",
    "axplat-aarch64-qemu-virt?/rtc",
    "axplat-riscv64-qemu-virt?/rtc",
    "axplat-loongarch64-qemu-virt?/rtc",
    "axplat-arm-qemu-virt?/rtc",
]
smp = [
    "axplat-x86-pc?/smp",
    "axplat-aarch64-qemu-virt?/smp",
    "axplat-riscv64-qemu-virt?/smp",
    "axplat-loongarch64-qemu-virt?/smp",
    "axplat-arm-qemu-virt?/smp",
    "axplat/smp",
]
tls = ["axcpu/tls"]
uspace = [
    "paging",
    "axcpu/uspace",
]

[lib]
name = "axhal"
path = "src/lib.rs"

[dependencies]
axcpu = "0.3"
axplat = "0.4"
cfg-if = "1.0"
heapless = "0.8"
kernel_guard = "0.2"
lazyinit = "0.2"
log = "0.4"
memory_addr = "0.4"
percpu = "0.4"

[dependencies.axalloc]
path = "../axalloc"
optional = true

[dependencies.axconfig]
path = "../axconfig"

[dependencies.axlog]
path = "../axlog"

[dependencies.linkme]
version = "0.3.33"
optional = true

[dependencies.page_table_multiarch]
version = "0.6"
features = ["copy-from"]
optional = true

[build-dependencies.axconfig]
path = "../axconfig"

[target.'cfg(target_arch = "aarch64")'.dependencies]
aarch64-cpu = "11.2"

[target.'cfg(target_arch = "aarch64")'.dependencies.axplat-aarch64-qemu-virt]
version = "0.4"
optional = true

[target.'cfg(target_arch = "arm")'.dependencies.axplat-arm-qemu-virt]
version = "0.4"
optional = true

[target.'cfg(target_arch = "loongarch64")'.dependencies.axplat-loongarch64-qemu-virt]
version = "0.4"
optional = true

[target.'cfg(target_arch = "riscv64")'.dependencies.axplat-riscv64-qemu-virt]
version = "0.4"
optional = true

[target.'cfg(target_arch = "x86_64")'.dependencies.axplat-x86-pc]
version = "0.4"
optional = true
